@model IEnumerable<I.Models.Organization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrganizationCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Organization_Status.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hkey)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrganizationCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Organization_Status.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hkey)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
